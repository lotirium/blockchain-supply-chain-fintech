{
  "name": "shipment-project",
  "version": "1.0.0",
  "description": "Supply chain management system",
  "scripts": {
    "dev": "concurrently \"npm run dev:blockchain\" \"npm run dev:server\" \"npm run dev:web\" \"npm run dev:image-service\"",
    "dev:blockchain": "cd blockchain && npx hardhat node --hostname 0.0.0.0 --port 8555",
    "dev:server": "cd server && PORT=3011 npm run dev",
    "dev:web": "cd web && npm run dev",
    "dev:image-service": "cd image-service && ./venv/bin/python -m uvicorn src.main:app --host 0.0.0.0 --port 8000",
    "deploy:contracts": "cd blockchain && npx hardhat run scripts/deploy.js --network local"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
